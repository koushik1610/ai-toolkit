# Archetype: generic

The default. No domain assumptions. Bootstrap uses the base templates as-is.

**Seeded hard rules:** none beyond the template placeholders.
**Extra rule files:** none.
**Settings additions:** none beyond the base (Stop memory hook + git permissions).

Use when the project doesn't fit a more specific archetype, then add rules as patterns emerge.
